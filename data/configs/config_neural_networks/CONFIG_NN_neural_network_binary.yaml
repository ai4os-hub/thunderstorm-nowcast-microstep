model_parameters:
    nn_layer:
      - name: Conv1D
        other_settings: 5, (1, ) 
        activation: "'relu'"
        kernel_initializer: "'he_uniform'"
        input_shape: '(3, 2, 1)' #'(39, 1)' #len(data_X[0])

      #- name: MaxPooling2D
      #  other_settings: '(2, 2)'

      - name: Flatten

      - name: Dense
        other_settings: '80'
        activation: "'relu'"
        kernel_initializer: "'he_uniform'"

      - name: Dense
        other_settings: '2' #'13' #len(data_Y[0])
        activation:  "'sigmoid'"  #"'softsign'"

    optimizer:
        name: 'SGD'
        learning_rate: '0.01'
        momentum: '0.9'

    model_compile:
        optimizer: ''
        loss: "'binary_crossentropy'"
        metrics: "['accuracy']"

train_model_settings:
    n_folds: 5
    epochs: 50 #20 #10
    batch_size: 32
    output_neurons: 1
    num_classes: 2
    class_weight: {0: 1., 1: 10000.}
